services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8084:8084"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://dpg-daapsi8ae00c73ev01j0-a.frankfurt-postgres.render.com:5432/ouou
      SPRING_DATASOURCE_USERNAME: ouou_user
      SPRING_DATASOURCE_PASSWORD: okqrAhYkmqyyLfoFijBTcAfyRFikMn5j
      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - app
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped

networks:
  appnet: